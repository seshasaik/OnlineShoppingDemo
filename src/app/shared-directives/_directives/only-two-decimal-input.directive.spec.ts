import { OnlyTwoDecimalInputDirective } from './only-two-decimal-input.directive';

describe('OnlyTwoDecimalInputDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyTwoDecimalInputDirective();
    expect(directive).toBeTruthy();
  });
});
