import { SumProductQuantityPipe } from './sum-product-quantity.pipe';

describe('SumProductQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new SumProductQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
