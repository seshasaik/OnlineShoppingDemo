import { MaxProductPricePipe } from './max-product-price.pipe';

describe('MaxProductPricePipe', () => {
  it('create an instance', () => {
    const pipe = new MaxProductPricePipe();
    expect(pipe).toBeTruthy();
  });
});
